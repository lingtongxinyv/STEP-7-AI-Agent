# STEP 7 AI Agent 使用文档

一个接入大语言模型的西门子 PLC 编程助手：支持 S7-200 SMART 等主流 PLC 的程序生成、变量读写、连机诊断，内置模拟 PLC（无硬件即可练习）、梯形图查看与 .awl 导出，可打包为单个 exe 或使用项目内置 Python 运行时在其他电脑直接运行。

> 返回项目说明：[README.md](README.md)

---

## 目录

1. [快速开始](#1-快速开始)
2. [安装](#2-安装)
3. [配置](#3-配置)
4. [界面与功能](#4-界面与功能)
5. [常用对话示例](#5-常用对话示例)
6. [变量地址格式](#6-变量地址格式)
7. [内置程序模板](#7-内置程序模板)
8. [梯形图与 .awl 导出（接入 Micro/WIN SMART）](#8-梯形图与-awl-导出接入-microwin-smart)
9. [常见故障排查](#9-常见故障排查)
10. [安全注意事项](#10-安全注意事项)

---

## 1. 快速开始

只需 3 步即可体验完整流程（无需任何 PLC 硬件）：

1. **启动程序**：双击 `Step7AIAgent.exe`，或在项目目录双击 **`启动.bat`**（使用项目内置 Python 运行时，无需安装 Python）。首次运行会自动生成 `config.json`。
2. **启动模拟 PLC**：在右侧 **PLC 面板 → 模拟 PLC** 区域，点击 **「启动模拟PLC」**。
3. **开始对话**：在左侧输入框试试：
   - 「用模板生成一个星三角降压启动程序，切换时间 8 秒」
   - 「读取 VW100 的产量值」
   - 「电机正反转怎么设计？」（可切换顶部**学习模式**获得引导式教学）

生成的程序会自动出现在右侧 **「程序」面板**：**「程序代码」页可查看复制代码，「梯形图」页可直观查看并导出 PNG，也可一键导出 Micro/WIN SMART 可导入的 `.awl` 文件**（见第 8 节）。

---

## 2. 安装

### 方式一：直接使用 exe（推荐，适合分发给其他电脑）

| 项目 | 说明 |
|---|---|
| 系统要求 | Windows 10/11 64 位 |
| 硬件要求 | 内存 ≥ 4 GB（使用云端模型时要求很低；使用本地 Ollama 模型建议 ≥ 8 GB + 独立显卡更佳） |
| 分发方式 | 把 `dist\Step7AIAgent.exe` 单独拷贝到任意电脑，双击即可运行 |

说明：

- exe 是**单文件绿色程序**，无需安装 Python 或任何依赖。
- 首次启动解压需要几秒钟；会在 exe 同目录生成 `config.json`（配置）与 `startup.log`（启动日志，排查问题用）。
- 使用**本地模型**时，目标电脑需另行安装 Ollama 并下载模型（见 3.2 节）；使用**云端 API** 时只需联网并填入 API Key。

### 方式二：使用项目内置运行时（无需安装 Python）

项目自带 `runtime\` 目录（内含 Python 3.11.9 与全部依赖），目标电脑**不装 Python 也能跑源码版**：

1. 把整个项目文件夹（含 `runtime\`）拷贝到目标电脑；
2. 双击项目根目录的 **`启动.bat`** 即可启动（后台运行、无命令行黑窗）；
3. 等价命令行：`runtime\pythonw.exe main.py`（排查问题时可改用 `runtime\python.exe main.py` 看到报错输出）。

说明：

- 运行时已隔离（启动脚本设置了 `PYTHONNOUSERSITE=1`），不受目标电脑已装 Python 的干扰。
- 如需重装/补装依赖：`runtime\python.exe -m pip install -r requirements.txt --ignore-installed`（打包用不到的可跳过 pyinstaller）。
- `runtime\` 体积约 500 MB，属正常（含 PySide6 完整界面库）。

> 注意：开源仓库不包含 `runtime\` 目录，可按 README 中的步骤自行构建。

### 方式三：从源码运行（自行准备 Python）

适合开发者或想修改功能的用户。

1. 安装 Python 3.11+（开发验证环境为 3.11.9）。
2. 安装依赖：

   ```powershell
   pip install -r requirements.txt
   ```

   核心依赖：`PySide6`（界面）、`python-snap7`（PLC 通信，纯 Python 实现，无需额外 DLL）、`openai`（模型接口）、`pyinstaller`（打包）。
3. 在项目根目录运行：

   ```powershell
   python main.py
   ```

> 注意：`python-snap7` 3.x 为纯 Python 协议实现，不需要再单独安装 snap7 动态库。

### 方式四：自行打包 exe

修改源码后可重新打包：

```powershell
powershell -ExecutionPolicy Bypass -File .\build_exe.ps1
```

产物输出到 `dist\Step7AIAgent.exe`。

---

## 3. 配置

### 3.1 两种配置方式

- **图形界面**（推荐）：工具栏 **「模型设置」** 打开设置对话框，改完自动保存。
- **配置文件**：exe 同目录的 `config.json`，可直接用文本编辑器修改（程序运行时修改会被界面覆盖，建议关闭程序后再改）。

### 3.2 模型配置

程序支持两类模型后端，通过 `provider` 字段或设置对话框切换：

**① Ollama 本地模型（默认，数据不出本机）**

前提：本机已安装 [Ollama](https://ollama.com) 并下载模型：

```powershell
ollama pull qwen3:8b     # 推荐，质量较好
ollama serve             # 启动服务（默认端口 11434）
```

对应配置：

```json
"llm": {
    "provider": "ollama",
    "ollama": {
        "base_url": "http://localhost:11434/v1",
        "model": "qwen3:8b"
    }
}
```

**② 云端 API（质量更好、响应更快，需联网）**

内置三家预设（在设置对话框直接选择自动填入）：

| 预设 | base_url | 默认模型 |
|---|---|---|
| DeepSeek | `https://api.deepseek.com` | `deepseek-chat` |
| 通义千问 | `https://dashscope.aliyuncs.com/compatible-mode/v1` | `qwen-plus` |
| 智谱GLM | `https://open.bigmodel.cn/api/paas/v4` | `glm-4-flash` |

对应配置（以 DeepSeek 为例）：

```json
"llm": {
    "provider": "cloud",
    "cloud": {
        "base_url": "https://api.deepseek.com",
        "model": "deepseek-chat",
        "api_key": "sk-你的密钥"
    }
}
```

其他参数：`temperature`（默认 0.3，越低越稳定）、`timeout`（默认 120 秒）。

**如何确认配置可用？** 点击工具栏 **「测试连接」**（测已保存配置），或在模型设置对话框点 **「测试连通性」**（修改地址/Key 后无需保存即可测试）。测试会依次验证：服务连通与鉴权 → 模型名称是否存在 → 发起一次极简推理，并显示服务响应耗时。失败时按提示检查 Ollama 是否启动、API Key、模型名拼写。

> **本地 vs 云端怎么选？** 小参数本地模型（如 1.7b）对工具调用遵从度差，可能出现参数传错、擅自改程序的情况；追求程序质量建议 8b 及以上本地模型，或直接使用云端模型。工程模式（直接出程序）推荐云端。

### 3.3 PLC 连接配置

默认配置面向 S7-200 SMART 真机：

```json
"plc": {
    "profile": "S7-200 SMART",
    "host": "192.168.2.1",
    "rack": 0,
    "slot": 1,
    "port": 102,
    "allow_write": false
}
```

各型号推荐参数（界面下拉选择型号后会自动填充）：

| 设备型号 | Rack | Slot | 默认 IP |
|---|---|---|---|
| S7-200 SMART | 0 | 1 | 192.168.2.1 |
| S7-200 | 0 | 2 | 192.168.2.1 |
| S7-300/400 | 0 | 2 | 192.168.0.1 |
| S7-1200/1500 | 0 | 1 | 192.168.0.1 |

- **模拟 PLC** 无需配置：固定使用 `127.0.0.1:102`，客户端以 rack=0 / slot=2 连接，程序会自动处理。
- 电脑与 PLC 通信前，请确认电脑网卡与 PLC 在同一网段（例如把电脑 IP 改为 192.168.2.x）。

### 3.4 完整默认配置（config.json）

```json
{
  "llm": {
    "provider": "ollama",
    "ollama": { "base_url": "http://localhost:11434/v1", "model": "qwen3:8b" },
    "cloud": { "base_url": "https://api.deepseek.com", "model": "deepseek-chat", "api_key": "" },
    "temperature": 0.3,
    "timeout": 120
  },
  "plc": {
    "profile": "S7-200 SMART",
    "host": "192.168.2.1",
    "rack": 0, "slot": 1, "port": 102,
    "allow_write": false,
    "poll_interval": 1000
  },
  "mode": "engineering"
}
```

`mode`：`engineering` 工程模式（直接给完整程序）/ `learning` 学习模式（引导提问、给提示）。

---

## 4. 界面与功能

主界面为左右分栏：**左侧对话区 + 右侧功能 Tab（PLC / 程序）**。

### 4.1 工具栏（顶部）

| 控件 | 功能 |
|---|---|
| 模式下拉框 | 切换 工程模式 / 学习模式 |
| 测试连接 | 对当前生效配置执行连通性测试：服务可达性 → 鉴权 → 模型是否存在 → 极简推理，弹窗显示结论与响应耗时 |
| 模型设置 | 打开模型配置对话框（内含「测试连通性」，可测尚未保存的填写内容） |
| 清空对话 | 清空当前对话历史（含工具记录） |
| 关于 | 版本信息 |

### 4.2 对话面板（左侧）

- 支持 Markdown 渲染（表格、代码块），AI 回答流式输出。
- **Enter 发送，Shift+Enter 换行**。
- AI 可自动调用工具（连接 PLC、读写变量、生成程序等），调用过程显示在状态栏。

### 4.3 PLC 面板（右侧 Tab 1）

- **模拟 PLC**：点击「启动模拟PLC」即可在本机 127.0.0.1:102 起一个仿真 PLC，无需硬件。预置演示数据：VW100=今日产量(128)、VW102=目标产量(500)、VD120=炉温(36.5℃)、VB200=设备状态、VW210=报警码、M0.0=运行标志。
- **真机连接**：选型号 → 填 IP → 点「连接」。连接状态实时显示。
- **安全门禁**：默认**只读**（一切写入被拒绝）。勾选「允许写入」后，每次写入仍会**弹窗逐条确认**（双保险）。
- **变量监控**：输入地址（如 VW100、Q0.0）添加监控行，可手动刷新或勾选「自动刷新(1秒)」；选中行后可在下方输入值执行「写入选中行」。

### 4.4 程序面板（右侧 Tab 2）

- AI 调用模板生成程序后，自动填充四个标签页：
  - **程序代码**：STL/SCL 源码，可一键复制或导出为 .stl/.scl 文件；
  - **梯形图**：按模板逻辑绘制的梯形图（母线/触点/线圈/定时器框），可滚动查看，点击「梯形图存PNG」导出图片；
  - **I/O 分配表**：地址与用途对照；
  - **原理与导入**：工作原理说明与导入步骤。
- 底部按钮：**复制代码** / **导出文件**（.stl/.scl）/ **导出AWL**（Micro/WIN SMART 可直接导入，见第 8 节）/ **梯形图存PNG**。
- 模板程序是经过验证的固定代码，**比 AI 手打的更可靠**，下载真机前仍须人工复核。
- SCL 模板（S7-1200/1500）不提供梯形图与 .awl（Micro/WIN 专有格式）。

---

## 5. 常用对话示例

| 你说 | AI 做 |
|---|---|
| 用模板生成一个星三角降压启动程序，切换时间8秒 | 调用模板工具生成（switch_pt=80），程序面板自动填充 |
| 读取 VW100 的值 | 调用读工具返回模拟 PLC 中的 128 |
| 把 Q0.0 置位 | （默认只读）提示需开启写入；开启后弹窗确认再写 |
| 现在产量离目标还差多少？ | 读取 VW100/VW102 后计算回答 |
| 传送带计数怎么编程？ | 调用 conveyor 模板生成完整程序 |
| 这是一个非标工况：…… | 学习模式给思路提示；工程模式直接写代码 |
| 我连不上PLC怎么办 | 结合当前连接状态给出排查建议 |

---

## 6. 变量地址格式

**S7-200 / 200 SMART 风格**（V 区自动映射为 DB1）：

| 写法 | 含义 |
|---|---|
| `I0.0` / `Q0.1` / `M0.0` | 输入 / 输出 / 中间继电器位 |
| `VB100` | V 区字节（BYTE） |
| `VW100` | V 区字（INT，2字节） |
| `VD100` | V 区双字（默认 REAL 实数，4字节） |
| `VD100:dint` | 显式指定双字为有符号整数 |

**标准 S7 风格**（S7-300/400/1200/1500）：

| 写法 | 含义 |
|---|---|
| `DB1.DBX0.0` | DB 块位 |
| `DB1.DBB2` / `DB1.DBW4` / `DB1.DBD6` | DB 块字节 / 字 / 双字 |
| `MD20` / `MW20` / `MB20` | M 区双字（默认 REAL）/ 字 / 字节 |
| `MD20:dword` | 显式指定类型 |

---

## 7. 内置程序模板

| 模板 key | 名称 | 语言 | 说明 |
|---|---|---|---|
| motor_latch | 电机启保停控制 | STL | 基础起停自锁电路 |
| motor_fwd_rev | 电机正反转控制 | STL | 带按钮/接触器双重互锁 |
| star_delta | 星三角降压启动 | STL | 切换时间可调（参数 switch_pt，100ms 时基，80=8秒） |
| flasher | 闪烁电路 | STL | 周期可调的通断脉冲 |
| traffic_light | 交通灯控制 | STL | 定时器级联的多灯循环 |
| conveyor | 传送带计数控制 | STL | 光电计数 + 定长停机 |
| dual_belts | 双皮带连锁控制 | STL | 顺启逆停连锁 |
| shuttle | 往返小车控制 | STL | 限位开关自动往返 |
| motor_scl | 电机启保停（SCL 版） | SCL | 面向 S7-1200/1500 的 TIA 写法 |

使用方式：直接对话描述需求，AI 会自动选择模板并传参；也可以明确说「用 xx 模板」。

---

## 8. 梯形图与 .awl 导出（接入 Micro/WIN SMART）

AI 生成程序后（模板程序或直接编写的标准 STL），程序面板即可查看梯形图并导出，无需手抄代码：

### 8.1 查看与导出梯形图

1. 让 AI 生成程序（模板程序，或 AI 直接编写的标准 STL 程序均可）；
2. 切到程序面板 **「梯形图」** 标签页：左右母线、常开/常闭/边沿触点、输出线圈、TON/CTU 功能框自动绘制，梯级编号与代码中 NETWORK 编号一一对应；同一网络多个输出会垂直挂接在输出侧竖母线上；
3. 点击 **「梯形图存PNG」** 可把当前梯形图导出为图片（用于报告、存档、打印）。

**可自动转换的 STL 指令**：LD/LDN、O/ON、A/AN、EU/ED、NOT、`=`（可多个）、TON/TOF/TONR/TP、CTU/CTD、S/R。
含 FOR/NEXT、MOV、间接寻址等无法可靠平面化的指令时，梯形图页会提示暂不支持（仍可用程序代码页与导出 AWL）。

### 8.2 导出 .awl 并导入 Micro/WIN SMART

点击程序面板的 **「导出AWL」**，得到 `.awl` 文件（ANSI/GBK 编码，含中文注释），然后：

1. 打开 **STEP 7-Micro/WIN SMART**，新建项目并选择实际 CPU 型号；
2. 菜单 **「文件(File) → 导入(Import)」**，选择导出的 `.awl` 文件；
3. 导入后程序出现在主程序块中，按 **Ctrl+F4 编译**（或菜单「编辑→编译」），确认零错误零警告；
4. 首次使用建议先用**状态图表**验证 I/Q 逻辑，确认无误后再考虑下载真机。

> 说明：
> - `.awl` 内容即模板的 STL 语句表（NETWORK + 指令行），Micro/WIN SMART 导入后自动生成对应网络；
> - 若「导入」菜单不可用，可退而求其次：打开「程序代码」页复制代码，在 Micro/WIN SMART 的 **STL 视图**中粘贴；
> - SCL 模板（S7-1200/1500）面向 TIA Portal，不提供 .awl 导出。

---

## 9. 常见故障排查

### 9.1 程序无法启动 / 启动后无窗口 / 闪退

1. **查看 `startup.log`**（exe 同目录）。正常应看到最后一行为 `entering event loop...`：
   - 若停在 `importing PySide6...` → 运行库被杀毒软件拦截，把 exe 加入白名单后重试。
   - 若出现 `EXCEPTION:` 堆栈 → 按堆栈信息定位，或把日志内容反馈给开发者。
2. **对话过程中闪退（曾出现于本地 Ollama 模型）**：已修复——程序安装了全局异常钩子，任何线程的未捕获异常都会**写入日志并弹窗提示**，而不再无提示退出。若仍遇到：
   - 查看 exe 同目录 `startup.log` 末尾的「未捕获的异常」或「线程 … 未捕获异常」条目；
   - 若存在 `fault.log`，说明发生了原生层崩溃（如显卡驱动/运行库问题），把文件内容反馈给开发者；
   - 确认 Ollama 服务正常（`ollama list` 能列出模型）；小参数模型（≤2b）输出格式不稳定，容易触发工具参数解析失败——该情况现在也只会显示错误消息而不会崩溃，但建议换 qwen3:8b 及以上模型。
3. **首次启动慢（几秒到十几秒）属正常**：单文件 exe 需先解压到临时目录。
4. 杀毒软件误报：PyInstaller 打包的程序偶发误报，添加信任即可。
5. 使用 `启动.bat` 双击后无反应：改用命令行 `runtime\python.exe main.py` 启动查看报错。

### 9.2 模型相关

| 现象 | 原因与解决 |
|---|---|
| 「无法连接模型服务」 | Ollama 未启动：运行 `ollama serve`；云端则检查网络与 base_url |
| 「模型不存在」 | 本地未下载该模型：`ollama pull qwen3:8b`；或检查模型名拼写 |
| 「模型鉴权失败」 | 云端 API Key 错误/过期，在「模型设置」中重新填写 |
| 「模型响应超时」 | 本地模型首问需加载（大模型可能 30~60 秒），后续会变快；仍超时可在设置中增大 timeout |
| 回答质量差、乱编程序、不调工具 | 换更大参数模型（本地 qwen3:8b 及以上）或改用云端 DeepSeek 等；工程模式对模型要求更高 |
| Ollama 端口冲突 | 修改 base_url 端口与 `ollama serve` 启动参数一致 |

### 9.3 PLC 连接相关

| 现象 | 原因与解决 |
|---|---|
| 模拟 PLC 启动失败 | 102 端口被占用：关闭其他占用 102 端口的程序（如其他 snap7 服务/STEP7 在线工具）；重启程序再试 |
| 真机连接超时 | ① 检查网线与电脑网卡 IP 是否与 PLC 同网段；② `ping PLC的IP` 测通断；③ 确认 PLC 上电且未达最大连接数 |
| 真机连接被拒绝 | Rack/Slot 不对：SMART 用 0/1，S7-300 用 0/2；S7-1200/1500 需在 TIA 中勾选「允许来自远程对象的 PUT/GET 通信访问」 |
| 连上但读取报错 | 地址写法不合法（见第 6 节）；V 区变量确认在 PLC 中确实存在 |
| 状态栏一直「正在思考」不返回 | 模型仍在生成（本地大模型首问较慢），或工具调用循环卡在重连 PLC，稍候或重启程序 |

### 9.4 写入与安全

| 现象 | 说明 |
|---|---|
| 「写入被拒绝：当前为只读安全模式」 | 默认行为。确需写入：PLC 面板勾选「允许写入」 |
| 每次写入都弹确认框 | 设计行为（逐条人工确认），无法绕过 |
| 写入后值不对 | 检查数据类型：VW 用整数、VD 默认实数；写入字符串「true/false」仅对位地址有效 |

### 9.5 程序生成相关

| 现象 | 解决 |
|---|---|
| 生成的程序参数不对（如切换时间没变） | 在对话中明确指出，AI 会用正确参数 key 重新生成；也可自己核对模板参数表 |
| 对话里的代码与程序面板不一致 | **以程序面板为准**（模板代码经过验证）；对话复述可能有手抄误差 |
| 导入 Micro/WIN SMART 编译报错 | 确认 .awl 导入时 CPU 型号匹配、T37/T38 等定时器编号未被其他网络占用；或改用 STL 视图粘贴 |
| 梯形图页为空 / 梯形图存PNG灰 | 程序为 SCL（S7-1200/1500，不提供梯形图），或 STL 含无法转换的指令（如 FOR/NEXT、MOV、间接寻址）；此时仍可复制代码与导出 AWL |
| 导出文件在哪里 | 「导出」按钮会弹保存对话框，自选位置保存为文本文件 |

### 9.6 打包相关（开发者）

| 现象 | 解决 |
|---|---|
| 打包报 PyInstaller 未找到 | `pip install pyinstaller`，或确认用的 Python 3.11 环境 |
| exe 启动卡住无窗口 | 检查是否在代码中使用了 `threading.Lock` 等在 frozen 环境可能异常的结构（本项目已修复）；用 `startup.log` 定位卡点 |
| exe 体积大（约 100MB） | 正常：含 PySide6 + snap7 + openai 完整运行时 |

---

## 10. 安全注意事项

1. **真机写入有风险**：默认只读；开启写入后请确认弹窗中的地址与数值，避免误写到运行中的设备。
2. **程序下载前必须验证**：AI 生成的程序必须先在离线/仿真环境编译验证，真机首次运行请点动试车，并人工复核急停、互锁、过载保护等安全逻辑。
3. **安全功能不能只靠程序**：急停、安全门等必须采用硬件安全回路。
4. **API Key 保管**：`config.json` 中明文保存 API Key，请勿把含密钥的配置文件随 exe 分发（分发前删除 config.json，首次运行会自动重新生成）。

---

## 附：项目目录结构

```
step7 AI Agent/
├── main.py                  # 程序入口（含全局异常钩子与 fault.log 原生崩溃日志）
├── 启动.bat                 # 一键启动（使用内置 runtime，无需安装 Python）
├── app/
│   ├── core/config.py       # 配置管理（config.json 与 exe 同目录）
│   ├── plc/
│   │   ├── profiles.py      # 设备型号配置（4 类）
│   │   ├── address.py       # 变量地址解析
│   │   ├── driver.py        # snap7 真机驱动
│   │   └── mock.py          # 内置模拟 PLC（snap7 Server）
│   ├── programs/
│   │   ├── templates.py     # 程序模板库（9 个）
│   │   ├── stl_parser.py    # STL → 梯形图解析
│   │   └── ladder.py        # 梯形图模型/绘制控件 + .awl 导出
│   ├── agent/
│   │   ├── prompts.py       # 双模式系统提示词
│   │   ├── connectivity.py  # 模型连通性测试
│   │   ├── tools.py         # 工具定义与执行器（含安全门禁）
│   │   └── assistant.py     # 对话核心（流式 + 工具循环）
│   └── ui/
│       ├── main_window.py   # 主窗口（对话/PLC/程序面板）
│       └── workers.py       # 后台线程 Worker
├── requirements.txt         # 依赖
├── build_exe.ps1            # 一键打包脚本
└── dist/Step7AIAgent.exe    # 分发用可执行文件
```
